<% if @records.blank? %>
  <p>Books are not currently in the system.</p>
<% else %>
  <p>These are the current books in our system</p>

  <ul id = "books">
    <% @records.each do |c| %>
      <li><%= link_to c.number, {:action => 'show', :id => c.id} -%></li>
      <b><%= c.number %></b>
    <% end %>
  </ul>

<% end %>
<p><%= link_to "Update a Record", {:action => 'new' }%></p>
